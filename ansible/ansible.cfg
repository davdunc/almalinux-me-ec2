# Keep to ansible-core built-ins here: EL10 ships ansible-core 2.16 and
# fancy third-party callbacks break across collection versions.
[defaults]
inventory = inventory/localhost.yml
roles_path = roles
interpreter_python = auto_silent
retry_files_enabled = False
